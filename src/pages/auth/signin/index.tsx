import React from "react";
import AuthenticationForm from "./AuthenticationForm";

const SigninPage = () => {
  return (
    <main className="flex items-center justify-center h-screen">
      <AuthenticationForm w={400} />
    </main>
  );
};

export default SigninPage;
